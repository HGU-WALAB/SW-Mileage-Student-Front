import { Box, styled } from '@mui/material';

export const Layout = styled(Box)({
  padding: '30px',
  display: 'flex',
  flexDirection: 'column',
  alignItems: 'center',
  gap: '50px',
  minWidth: '700px',
  overflowX: 'scroll',
  px: '50px',
});
